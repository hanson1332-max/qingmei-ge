# 清美 GE 矩阵

正式源码位置：`Code/Git/qingmei-ge`。2026-09-04 已初始化本地 Git 仓库；尚未推送或关联远程仓库。`Code/清美GE矩阵` 和原 `AI/outputs` 地址保留兼容链接，不是第二份程序。当前甘特图仍位于 `Code/project-gantt`，构建脚本兼容该位置和未来的 `Code/Git/project-gantt`。

- 主工具：`清美GE矩阵搭建器.html`。
- 历史版本、`qingmei-ge-dashboard`、`work`、Excel 兼容链接与检查文件已归入 `归档/2026-09-04_过程文件`，未删除。
- 历史 Excel 实体仍在 Data 的 `历史底表`，不作为最新研究结论。

## 2026-09-04 迁移

文件实体从 `AI/outputs/01a06515-fed5-7782-8354-d43c567bfd6e` 迁至此处；原目录保留符号链接，不是两份程序。没有删除或覆盖工具文件。

浏览器中的修改不是 HTML 文件的一部分。本次没有读取、导出或迁移浏览器 localStorage，不可把历史 Excel 或内置模板当成当前数据。

迁移使用步骤：

1. 继续在原来已打开的网页中点击“导出 JSON”。不要先关闭或重置该页面。
2. 将下载的 JSON 保存至 `/Users/yuhan/Library/CloudStorage/OneDrive-个人/Data/QingMei/赛道研究/GE矩阵/JSON备份`，建议文件名带日期。
3. 从 Code 新地址打开主工具，点击“导入”，选择备份；核对赛道、分类、维度和修改过的数值。
4. 确认一致后再更新浏览器书签。原地址保留作兼容，不自动跳转。

阶段结论、评分依据和分享版快照应随研究记录归入 Note 的清美项目目录。本次未新建研究结论或伪造最新数据备份。

## 云端版进展（2026-09-04）

已收到用户导出的 `/Users/yuhan/Downloads/清美.json`：20 赛道、4 分类（西式快餐、中式快餐、正餐、小吃）、8 个评分维度。未擅自合并赛道或重打分。完整备份已保存至 Data 的 `JSON备份/清美_2026-09-04_云端迁移前.json`。

`cloud` 是云端适配源码；`node cloud/build.mjs` 从当前单文件工具生成 `public` 静态发布目录。构建读取相邻甘特图项目的公开 Supabase 客户端配置，不使用管理密钥。线上产物不内嵌旧模板或用户 JSON；数据通过独立 GE 数据表读取。

`node cloud/test.mjs <JSON路径>` 验证脚本语法、JSON、访客不写入、管理员保存、冲突和断网状态。已通过本地模拟检查，尚未进行真实登录或数据库写入测试。

### 上线前仍须完成

1. 在原甘特图的 Supabase 项目 SQL 编辑器执行 `cloud/setup.sql`（一次性建表；如已有同名表将回滚，不覆盖）。
2. 在 Auth 用户列表找到原管理员的 UUID，将其加入 `ge_matrix_admins.user_id`。数据库权限才是最终权限，不只依赖页面按钮。
3. 发布 `public` 到专用 GitHub Pages 仓库；仅发布该目录，不上传归档、原始 JSON、本地模板或 SQL。
4. 将正式网页完整地址加入 Supabase Auth Redirect URLs。新工具使用单独会话存储，不复用甘特图本机登录状态。
5. 管理员在新网页登录并导入最新备份 JSON。此步骤会发布数据给访客阅读。核对人数、指标、数值后，再进行匿名拒写、管理员写入和版本冲突的真实验证。

当前状态：本地云端版已生成；缺 Supabase 管理连接，未建表、未发布 GitHub Pages、未写入云端数据。不要将 `public/index.html` 当作已上线链接。
